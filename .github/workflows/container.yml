name: deploy-container

on:
  push:
    branches:
      - master
    paths:
      - 'packages/container/**'

defaults: 
  run: 
    working-directory: packages/container

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - run: npm install
      - run: npm run build
      
      - uses: shinyinc/action-aws-cli@v1.2
      - run: aws s3 sync dist s3://rushi-mfe/container/latest
        env: 
          AWS_ACCESS_KEY_ID: AKIA4KQWEF3DZEYER26D
          AWS_SECRET_ACCESS_KEY: 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
          AWS_DEFAULT_REGION: 'us-east-1'